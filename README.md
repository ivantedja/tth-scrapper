## Install Dependency

`$ bundle install`

## Setup env

`$ cp env.sample .env`

## Run script

`$ ruby main.rb`

## Improvements

- automatically retrieve topics
- create the UI
- automatically create folders based on topic, etc
- better video's filename